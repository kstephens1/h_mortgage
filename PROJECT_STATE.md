# Project State

## 2026-07-30
- Implemented the private-access application boundary. The orchestrator now exposes `POST /auth/login`, verifies a scrypt-derived salted password hash with constant-time comparisons, issues signed 12-hour bearer sessions, and protects `/health`, `/chat`, and the Nginx auth-verification endpoint. Passwords are never returned or persisted by the browser.
- Added origin-restricted CORS, `Authorization` support, 64 KiB request limits, and conservative per-client login/chat rate limits. The MCP server now returns CORS access only to configured origins and remains suitable for a loopback-only production listener.
- Added a login-only initial screen, session-token storage in `sessionStorage`, automatic expiry/401 handling, logout, authenticated chat and direct MCP requests, and build-time production API/MCP endpoint configuration.
- Added unit and integration coverage for credential verification, token tampering and expiry, protected health access, login rejection/success, and rejected browser origins.
- Added explicit Firebase Hosting configuration for only `chat-h-prod-ks-2026`, dedicated loopback-only systemd units, an isolated Nginx virtual host with MCP auth subrequests and strict CORS, first-certificate bootstrap, versioned VM release installation, production deployment/smoke scripts, existing-application preflight checks, and a Chat-H-only rollback script.
- Added the production runbook, documented protected local setup, added a helper that generates salted local authentication values without persisting the password, and locked the production orchestrator to its configured loopback MCP endpoint to prevent authenticated endpoint overrides.
- Created the exact isolated Firebase project and deployed release `20260730T150623Z-0e0b7e2`. The UI is live at `https://chat-h-prod-ks-2026.web.app`; the protected API/MCP host is live at `https://chat-h.35.211.52.83.nip.io` with a Let’s Encrypt certificate expiring 2026-10-28 and automatic renewal enabled.
- Production verification passed for wrong-login rejection, correct login, protected health, anonymous MCP rejection, MCP initialize/list/tool call, deterministic protected chat, allowed/rejected CORS origins, the Firebase login shell, and compiled production endpoints. Token tampering/expiry and logout/session-storage behavior are covered by local checks. In-app visual automation could not initialize, so responsive behavior remains covered by structural CSS assertions and the successful production build.
- Post-deployment checks confirmed `wealthtrack` and `stock-picker` remained active on their original ports 4001 and 4000, while Chat-H listens only on loopback ports 8787 and 5174. The WealthTrack public API had a pre-existing certificate mismatch; its prior fallback response was preserved by loading the isolated Chat-H virtual host after the existing sites without editing or restarting WealthTrack.
- Updated three vulnerable transitive packages to their compatible patched versions; both local and deployed production dependency audits now report zero known vulnerabilities.
- Final verification passed: shell syntax checks, client static/authentication integration checks, all 14 Vitest tests, typecheck, MCP build, production-configured Vite build, production smoke tests, and existing-application regression checks.
- Fixed production chat requests being rejected with `MCP endpoint override is disabled`. When endpoint locking is enabled, the orchestrator now ignores the browser-supplied public MCP URL and always routes its server-side MCP calls to the configured loopback endpoint; unlocked local development still accepts the UI endpoint.
- Updated future release installation to preserve the existing session-signing secret, so routine deployments no longer invalidate active 12-hour browser sessions. The endpoint-lock regression smoke now sends the public browser MCP URL and passes against production.
- Prepared the private-production/authentication release for source control. The staged snapshot passed Git diff checks and a credential-pattern scan; no `.env` files, private keys, API tokens, or credential artifacts are tracked. Local verification passed all 14 Vitest tests, typecheck, the MCP build, client static and authentication integration checks, the client production build, and shell syntax checks.

## 2026-07-23
- Updated the root and test-client READMEs with a no-ngrok local-development guide. It covers dependency installation, running the MCP server on `127.0.0.1:8787`, running the direct browser UI on `127.0.0.1:5173`, and the optional OpenRouter-backed chat mode on local port `5174`.
- Documented the boundary that ChatGPT developer mode cannot access a local loopback endpoint; a public HTTPS URL is only needed for that separate hosted integration, not for local MCP/front-end development.
- Clarified that `test-client`'s `npm run dev:ui` intentionally starts no chat API; chat-composer requests then fail against port `5174`. The direct MCP controls remain available in this no-key local mode, while `npm run dev` starts the full chat stack.
- Corrected the local-development guides after identifying that the primary user workflow is the chat UI with an existing OpenRouter key. Both READMEs now make `test-client/npm run dev` the standard front-end command and position `dev:ui` only as a direct-MCP, no-chat troubleshooting mode.
- Changed the chat-mode boundary: without the **HSBC Mortgages** `@` chip, the OpenRouter chat is now general-purpose and receives no MCP tool schemas or mortgage-specific system instructions. Selecting the chip enables the restricted, MCP-backed mortgage mode. Updated the generic welcome, placeholder, header, and test-client documentation to match.
- Removed the manual **Mortgage Details** input/tool panel from the test client. Restyled the remaining chat, connection, and result panels in a dark ChatGPT-inspired layout with a navigation rail, centered chat surface, pill composer, and responsive side inspector. Updated the local-development docs to remove now-obsolete manual **Run Tool** instructions.
- Replaced the four static product-rate fixtures with a deterministic illustrative product finder. It derives LTV from property value and borrowing, applies LTV premiums across 2/3/5-year fixed and 2-year tracker products, calculates monthly repayments over the selected 2–40-year term, and estimates APRC/reversion rates. The HSBC-mode client exposes matching property, borrowing, and term fields and renders detailed product cards. All outputs are explicitly demo-only, not live HSBC offers.
- Restyled generated product cards to mirror the supplied HSBC examples: white product sheets, five-column rate summary, LTV/feature rows, details and glossary actions, and an in-place, non-transactional red **Switch your deal** button for each illustrative product.
- Added terminal-style composer history: with an empty message field, **↑** cycles backward through earlier user messages and **↓** moves forward, restoring the blank draft after the newest entry.
- Replaced the navigation-rail text mark with the user-supplied logo asset at `test-client/assets/chatgpt-logo.png`.
- Hardened the HSBC chip mode boundary. In HSBC mode, non-mortgage/capability questions bypass the model and return a deterministic response limited to repayment estimates and the illustrative deal finder; generic assistant replies are no longer possible while the chip is selected.
- Added HSBC-mode decision-in-principle routing. Mortgage-decision/AIP/DIP requests return a local, HSBC-badged response with a **Get a decision in principle** link button that opens `https://www.hsbc.co.uk/mortgages/decision-in-principle/` in a new tab.
- Removed the visible **Find mortgage deals / Illustrative** card and its direct form. Deal searches remain available through the HSBC chat and render the same product cards in the conversation.
- Added a conversational mortgage-journey selector in HSBC mode. Deal searches now offer all seven HSBC-style journey choices, preserve the selected journey in the product-finder input, and reuse the existing illustrative LTV/term pricing algorithm for each one.
- Wired product tool results into the chat product-card renderer, so successful product searches display the HSBC-style white deal sheets in the conversation as well as updating the inspector.
- Added the `get_customer_support` MCP tool for official-source-linked general and mortgage support contact options. It returns local phone-detail snapshots with explicit source-of-truth links; in HSBC mode, support phrases such as “I need help” and “contact the bank” can invoke the tool, while generic chat remains tool-free.
- Fixed mortgage-deal intent routing so concise requests such as “mortgage deals” show the seven-option journey chooser instead of falling through to a model prompt for inputs.
- Made each new deal-search request reset any prior mortgage-journey selection, so the chooser is consistently shown for repeated searches in the same chat.
- Added an in-chat animated **Thinking** indicator during chat requests. Its highlight sweeps across the text in a loop and is removed before the response or an error message appears.
- Routed complete HSBC product-search figures directly to the product MCP tool, avoiding an OpenRouter tool-calling failure observed when a provider response omitted its assistant message. Incomplete provider responses now retry once and report a clearer error if still invalid.
- Fixed product-search detail extraction to prefer the latest values in the conversation. Updated property values no longer combine with an earlier value and newer loan/deposit figures.
- Reduced the OpenRouter completion-token cap from 4,096 to 2,048 in the local client and default configuration. This avoids provider credit-reservation errors when the available balance is slightly below the former cap.
- Made standalone **I need help** requests return an expanded deterministic HSBC capabilities summary covering repayments, all mortgage journeys, product results, decisions in principle, customer support, and residential switching.
- Routed **contact HSBC** and equivalent explicit contact requests directly through `get_customer_support`, without OpenRouter. The MCP response now includes the published general, overseas, non-customer, and Premier phone routes plus official contact/chat and online-banking links; rendered website links always open in a new tab.
- Reduced HSBC-style product-card typography and spacing to align with the chat scale. For the `switch_residential` journey, every **Switch your deal** action now opens HSBC's existing-customer switching page in a new tab; other journeys retain their local demo action.
- Made OpenRouter completion sizing balance-aware. When the provider rejects the configured token cap and reports a lower affordable limit, the orchestrator automatically retries once at 90% of that limit instead of surfacing the credit-reservation error immediately.
- Fixed repayment requests being misrouted into an earlier mortgage-product journey. The orchestrator now derives the active workflow from the latest explicit intent, bounds extracted figures to that workflow, and calls `calculate_mortgage_repayment` directly when the required repayment inputs are complete.
- Expanded repayment tool text to echo loan amount, annual rate, term, and optional overpayment alongside standard monthly repayment and total interest. When an overpayment is supplied, the response also shows adjusted payment/interest plus interest and time saved.
- Constrained the desktop app to the viewport so the chat transcript scrolls independently while **Connection** and **Last Result** remain stationary. The existing below-chat inspector layout is retained at widths of 820px and narrower.
- Verification passed: the reported stale-deal conversation returned the repayment MCP result without OpenRouter or product cards; the missing-input route requested only loan, rate, and term; `npm --prefix test-client run check`, all 14 Vitest tests, typecheck, MCP build, and the Vite production build passed. In-app visual automation could not initialize, so fixed-pane behavior is covered by structural CSS assertions and the successful production build.

## 2026-07-15
- Read `AGENTS.md` as requested.
- Created this state file because no prior `PROJECT_STATE.md` existed in the repository.
- Added `test-client/`, a standalone browser chat UI for testing the MCP server over Streamable HTTP.
- Added `test-client/README.md` with run instructions.
- Verification passed: `npm --prefix test-client run check`, `npm test`, `npm run typecheck`, and `npm run build`.
- Smoke checked the running Vite client at `http://127.0.0.1:5173/` and confirmed a direct `tools/call` request to `http://127.0.0.1:8787/mcp` returns the expected mortgage result.
- Discussed how to make the test client conversational by adding a model-backed chat orchestrator between the browser UI and MCP server; no code changes made for that yet.
- Implemented a local `test-client` chat orchestrator using the OpenAI Responses API with default model `gpt-5.6-terra`; the browser chat now posts to `/chat`, and the orchestrator calls the existing MCP mortgage tool when the model requests it.
- Set the Vite dev server to strict port `5173` so it cannot auto-increment onto the orchestrator API port `5174`.
- Verification passed after orchestrator work: `npm --prefix test-client run check`, `npm test`, `npm run typecheck`, and `npm run build`.
- Started the local test client stack: Vite UI on `http://127.0.0.1:5173/`, orchestrator API on `http://127.0.0.1:5174/`; `/health` reports `gpt-5.6-terra`, and `/chat` returns the expected missing-token error until `OPENAI_API_KEY` or `OPENAI_OAUTH_TOKEN` is provided.
- Clarified that local OpenAI API testing should normally use `OPENAI_API_KEY`; `OPENAI_OAUTH_TOKEN` is only useful if the user already has a valid bearer token from a specific OAuth flow.
- Clarified that an OpenAI API key is itself used as a Bearer credential for API requests; extracting ChatGPT/browser session tokens is unsupported and should not be used for this local test client.
- Provided restart guidance for running the local MCP server and test-client orchestrator with `OPENAI_OAUTH_TOKEN`; no code changes made.

## 2026-07-16
- Confirmed the local MCP server is already running on `http://127.0.0.1:8787/mcp`.
- A `GET /mcp` response returned the expected MCP `406 Not Acceptable` (the endpoint requires an MCP-compatible `Accept` header), confirming the listener is available. Starting a second `npm run dev` instance correctly failed with `EADDRINUSE`.
- Diagnosed a test-client OpenAI OAuth failure: the supplied bearer token lacks the required `api.responses.write` scope. The chat orchestrator needs this permission to call the Responses API; use a suitable API key or obtain a correctly scoped OAuth token with appropriate organization and project access.
- Replaced the test-client's OpenAI Responses API orchestrator with OpenRouter Chat Completions, retaining the MCP function-calling loop. The default is the tool-capable Xiaomi FP8 model `xiaomi/mimo-v2-flash` because `xiaomi/fp8` identifies a provider/quantization, not an OpenRouter model ID.
- Added gitignored `test-client/.env` with an `OPENROUTER_API_KEY` placeholder. `npm run dev` and `npm run dev:api` now load it automatically through Node's `--env-file` option.
- Updated `test-client/README.md` with OpenRouter setup and environment documentation.
- Verification passed: `node --check test-client/server.js`, `npm --prefix test-client run check`, `npm test`, `npm run typecheck`, and `npm run build`.
- Added `find_mortgage_product_rates` to the HSBC MCP server. It accepts loan amount, property value, and deposit amount; derives LTV; and returns matching, explicitly illustrative HSBC-style local rate fixtures. It validates that loan plus deposit equals property value and never presents fixtures as live rates, product availability, eligibility, or offers.
- Reworked the test-client orchestrator from a hardcoded repayment function into allowlisted `tools/list` discovery. It exposes live MCP tool descriptions and input schemas to OpenRouter and routes each approved model tool call back to the correct MCP tool. The default `MCP_ALLOWED_TOOLS` includes repayment and product-rate tools.
- Updated test-client environment documentation and local `.env` with the allowlist; updated chat copy to describe approved multi-tool support. The direct browser panel remains repayment-specific.
- Verification passed: `npm test`, `npm run typecheck`, `npm --prefix test-client run check`, `node --check test-client/server.js`, and `npm run build`.
- Diagnosed a rate-query routing failure as a stale MCP process: the updated test-client API was running, but port `8787` still advertised only `calculate_mortgage_repayment`. Restarted the local MCP dev server; `tools/list` now confirms both `calculate_mortgage_repayment` and `find_mortgage_product_rates` are available.
- Added response-source badges in the test client: any assistant reply backed by an MCP `toolResults` entry, including direct `Run Tool` replies, displays an HSBC Mortgages logo chip. Model-only/generic assistant replies remain unbadged.
- Verification passed: `node --check test-client/src/app.js`, `npm --prefix test-client run check`, `npm test`, `npm run typecheck`, and `npm run build`.
- Implemented HSBC-chip restricted chat mode. The browser preserves a selected HSBC chip across sends in the current tab, sends `hsbcMode` with each chat request, and only clears the selection when the chip is deleted (or on refresh).
- The orchestrator now uses a mode-specific system prompt built from the approved MCP tools. In HSBC mode it calls matching tools, requests only missing tool inputs, and otherwise provides a capability-limited response without unsupported mortgage guidance or invented contact details. `/chat` now returns `source` as `mcp`, `hsbc-guidance`, or `generic`; both HSBC sources are logo-badged in the UI.
- Updated the test-client README with HSBC-mode behaviour. Verification passed: `node --check test-client/src/app.js`, `node --check test-client/server.js`, `npm --prefix test-client run check`, `npm test`, `npm run typecheck`, and `npm run build`.
- Added safe GitHub-flavoured Markdown rendering for assistant replies with local `marked` and `dompurify` dependencies. User messages and errors remain plain text; assistant Markdown is sanitized to a limited allowlist before DOM insertion, with safe external-link attributes.
- Styled rendered headings, lists, quotes, code, separators, links, and horizontally scrollable tables so tool output no longer displays Markdown syntax literally. Updated the assistant prompt to request concise GFM Markdown without raw HTML.
- Verification passed: `node --check test-client/src/app.js`, `node --check test-client/server.js`, `npm --prefix test-client run check`, `npm --prefix test-client exec vite build`, `npm test`, `npm run typecheck`, and `npm run build`.
- Initialized this previously non-Git workspace for publication. Confirmed `test-client/.env` is ignored by `.gitignore`; dependency directories and build outputs are also excluded from the initial commit.
- Created and pushed the initial `main` branch to `https://github.com/kstephens1/h_mortgage` (commit `a603625`). The staged secret scan found no API-key patterns, and `test-client/.env` was confirmed untracked before commit.
- Added an `@` mention selector to the test-client chat composer. Typing `@` opens a selectable **HSBC Mortgages** option; selecting it inserts an inline, non-editable HSBC-logo chip into the entry field while preserving the typed message text. Enter selects the option while the menu is open; Escape dismisses it.
- Reused `test-client/assets/hsbc-logo.png` for both the selector and selected chip. Verified static checks with `npm --prefix test-client run check` and `node --check test-client/src/app.js`; the running Vite server returned `200 OK` for the updated module and logo asset. In-app visual automation was unavailable because its browser connection could not initialize in this session.
- Fixed mention-chip deletion: the composer now detects when the HSBC chip has been removed, clears its selection state, and restores the default “Ask about a mortgage, or type @ to add HSBC Mortgages” placeholder even if the browser leaves an empty line-break node. Typing `@` again now reopens the selector.
- Verification passed: `node --check test-client/src/app.js`, `npm --prefix test-client run check`, `npm test`, `npm run typecheck`, and `npm run build`.
- Diagnosed an OpenRouter credit-limit response when using `xiaomi/mimo-v2.5`: without an explicit completion-token cap, the provider reserves the model maximum of 65,536 tokens, which exceeds the key's remaining-credit allowance of 49,119 tokens. Set a lower `max_tokens` value in the client request to run within the key's limit.
- Added configurable `OPENROUTER_MAX_TOKENS`, defaulting to `4096`, and send it as `max_tokens` on OpenRouter Chat Completions requests. Added `OPENROUTER_MAX_TOKENS=4096` to the local gitignored `test-client/.env` and documented it in the README.
- Verification passed: `node --check test-client/server.js`, `npm --prefix test-client run check`, `npm test`, `npm run typecheck`, and `npm run build`.
